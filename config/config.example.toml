# Masix Configuration Example
#
# Run 'masix config init' for interactive setup wizard
# The wizard will help you configure providers and models

[core]
data_dir = "~/.masix"
log_level = "info"

[telegram]
poll_timeout_secs = 60

[[telegram.accounts]]
bot_token = "YOUR_BOT_TOKEN_HERE"

[whatsapp]
enabled = false

[sms]
enabled = true

[mcp]
enabled = true

[[mcp.servers]]
name = "filesystem"
command = "npx"
args = ["-y", "@modelcontextprotocol/server-filesystem", "/data/data/com.termux/files/home"]

[[mcp.servers]]
name = "memory"
command = "npx"
args = ["-y", "@modelcontextprotocol/server-memory"]

# Providers will be configured by 'masix config init' wizard
[providers]
default_provider = ""

# Bot profiles (optional)
# [bots]
# [[bots.profiles]]
# name = "default"
# workdir = "~/.masix"
# provider_primary = ""
# provider_fallback = []

# Command execution
[exec]
enabled = true
allow_base = true
allow_termux = true

[policy]
rate_limit = { messages_per_minute = 60 }
